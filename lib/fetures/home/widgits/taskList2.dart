import 'package:flutter/material.dart';

class TaskList2 extends StatelessWidget {
  const TaskList2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("task list no 2 "),
    );
  }
}
